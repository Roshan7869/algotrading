---
name: config-backtest
description: Backtest configurations (9 configs)
version: 1.0.0
category: configuration
tags: [config, backtest, testing, freqtrade]
---

# Backtest Configurations

Collection of 9 backtest configuration files for different scenarios: default, optimized, aggressive, conservative, multi-pair, short-enabled, etc.

## Config Files
- `config_backtest.json` — Standard backtest
- `config_backtest_optimize.json` — Hyperopt-ready
- `config_backtest_multi_pair.json` — Multi-pair testing
- `config_backtest_short.json` — Short-enabled
- `config_backtest_aggressive.json` — Higher risk tolerance

## Trigger Phrases
- "use backtest config", "load backtest configuration"
- "switch backtest mode", "create test config"
